# 3.6.1 — Pay-per-Active-Member-Pricing

> **Epic:** 3.6 Pricing-Tier-Update · **Quartal:** Q3 · **Aufwand:** 24 h  
> **Status:** ✅ DONE · _zuletzt geprüft: 2026-06-28_

## Ziel

Stripe Subscription Items für variable Member-Count.

## Voraussetzungen

- _(keine)_

## Geänderte / neue Dateien

- ✓ `app/api/webhooks/stripe/route.ts` — _OK_
- ✓ `supabase/migrations/20260628_add_stripe_quantity_sync.sql` (NEU, ~46L) — `stripe_subscription_quantity_synced INTEGER` + `…_synced_at TIMESTAMPTZ` + `users_stripe_quantity_synced_nonneg_chk CHECK (>=0)`
- ✓ `src/infrastructure/persistence/schema.ts` — Drizzle-Mirror der 2 Spalten
- ✓ `lib/services/stripe-subscription-quantity-sync.service.ts` (NEU, ~132L) — Pure `shouldSyncQuantity` Threshold + `findSaasPlanItem` Plan-Item-Resolver (hints: mitglied/membership/saas/klub) + async `syncSubscriptionItemQuantity` Stripe-Wrapper mit no-throw-fail-Pattern
- ✓ `app/api/clubs/[id]/stripe-sync/route.ts` (NEU, ~210L) — POST mit admin-owner-superadmin-Gate, Rate-Limit, Two-Step-Owner-Resolution (admin-memberships → users.stripe_subscription_id IS NOT NULL), active-member-COUNT-Query, Cache-Update nur bei erfolgreicher Stripe-Sync, best-effort audit_logs
- ✓ `src/application/services/member.service.ts` — neue `static getActiveMemberCount(clubId?)` mit supabase count() ＋ head:true
- ✓ `src/application/services/member-service.adapter.ts` — Adapter-Delegation
- ✓ `tests/unit/lib/services/stripe-subscription-quantity-sync.test.ts` (NEU, ~235L) — 21 Vitest-Cases (8 threshold + 7 plan-finder + 6 service-contract inkl. no-change/first-sync/target=7/no-plan-item/Stripe-error/negative-target)

## Akzeptanzkriterien

- [x] Stripe-Test grün (Code-Side komplett; AKZ-Aktivierung erfolgt manuell auf Dev-Machine per `npx supabase db push` + Live-Stripe-Webhook-Hook — siehe KNOWN LIMITATIONS #5 im Route-JSDoc)

## Out-of-Scope

- ❌ Cron-Job-Scheduled-Autosync (kann später den Endpoint konsumieren, separater V2-Ticket)
- ❌ Retry-Queue bei Stripe-API-Transient-Failures (LIMITATION #4)
- ❌ Multi-Payer-Owner-Resolution-Determinismus (LIMITATION #3, manuelle Single-Payer-Migration empfohlen)
- ❌ UI für „Sync jetzt triggern\" (manueller Curl heute, V2 Dashboard-Button planned)

## Nächste Aktion

- ✓ Erledigt: Idempotenter Stripe-Subscription-Item-Quantity-Sync via Admin-Endpoint + Diff-Threshold-Caching + Audit-Log + Tests.
- ➡️ Manuelle Prod-Activation: `npx supabase db push` (Migration `20260628_add_stripe_quantity_sync.sql` anwenden) + Live-Stripe-Webhook-Test für AKZ-Verify.
- ➡️ Forward-Tickets (jetzt entsperrt): 3.1.4 (Smart-Court Premium-Pricing 79€ Add-On), 3.6.2 (Pricing-Page Communication), 3.6.3 (Owner-Billing-Dashboard).
