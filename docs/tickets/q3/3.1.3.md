# 3.1.3 — Smart-Court-Admin-UI

> **Epic:** 3.1 Smart-Court API · **Quartal:** Q3 · **Aufwand:** 16 h
> **Status:** ✅ DONE (vitest-AKZ-deferred) · _zuletzt geprüft: 2026-06-28_

## Ziel

Adapter-Konfiguration pro Court.

## Voraussetzungen

- `3.1.1` — ✅ DONE (siehe [`q3/3.1.1.md`](3.1.1.md))
- `3.1.2` — ✅ DONE (siehe [`q3/3.1.2.md`](3.1.2.md))

## Geänderte / neue Dateien

- ✓✓ `app/(protected)/admin/smart-court/page.tsx` — NEU, ~120L Server-Component (await cookies + createServerClient + admin-membership-lookup + clubs.features + courts.list + delegiert an Client-Component)
- ✓✓ `app/(protected)/admin/smart-court/smart-court-client.tsx` — NEU, ~285L Client-Component (RadioGroup-of-Cards mit Nuki/Shelly/Loxone + Save-Button mit Idempotency-Guard + Toast-Error-Mapping + Read-Only Court-List + aria-labels)
- ✓✓ `app/api/clubs/[id]/hardware-vendor/route.ts` — NEU, ~200L PUT (withApiAuth + verifyRole(admin) + club-scope-check + Zod-validate via `z.enum(HARDWARE_VENDORS)` + shallow-JSONB-Merge in clubs.features + audit_logs-Insert mit `actor_id` + missing_env-Detection via `HARDWARE_ENV_VARS[vendor]` + Idempotency-Check)
- ✓✓ `tests/unit/app/api/clubs/[id]/hardware-vendor/route.test.ts` — NEU, ~265L, **14 vitest-Cases** (auth-gate + club-scope + Zod-validation + idempotency + shallow-merge + missing-env + 5xx-error + audit-fail-non-blocking)
- ✓✓ `lib/hardware/adapter.ts` — MODIFIED, +7L HARDWARE_VENDORS const-array export als `as const satisfies readonly HardwareVendor[]` (Single-Source-of-Truth für Zod enums + UI-Card-Iteration + Webhook-Vendor-Validation)

## Akzeptanzkriterien

- [x] UI funktioniert — ✓ Native Code shipped, **Code-Reviewer-PASS R3 SHIP-ready**, TS-Baseline 0, vitest-AKZ dev-machine-gated (vitest MODULE_NOT_FOUND in sandbox wie 3.1.1/3.1.2)

## Out-of-Scope

- Per-Court-Vendor-Override (verfügbar in 3.1.4) — 1.0 nutzt Club-Level-Setting (alle Courts eines Clubs teilen denselben Vendor)
- Multi-Vendor-Setup pro Club (nicht in 1.0 geplant)
- Vendor-Health-Check (Token-Rotation, Connection-Pings) — separate Tickets

## Nächste Aktion

✓ _Audit-driven close 2026-06-28._ Optional: dev-machine `npx vitest run tests/unit/app/api/clubs/[id]/hardware-vendor/route.test.ts` zur AKZ-Verifikation.

## Acceptance-Resolution (2026-06-28)

**1. Native Code shipped** (5 Files, ~880 LOC gesamt):

- `app/(protected)/admin/smart-court/page.tsx` (~120L) — Server-Component mit async-cookies-Pattern + createServerClient + admin-membership-resolution (`.in('role',['admin','owner','superadmin'])` + `is_active:true`) + clubs.features + courts.list parallel-fetch + delegiert an Client-Component mit typed-props für discriminated-union `HardwareVendor`
- `app/(protected)/admin/smart-court/smart-court-client.tsx` (~285L) — `<Radiogroup>` mit 3 `<button role="radio" aria-checked aria-label>` Cards (Nuki Lock/Shelly Lightbulb/Loxone Cpu) + Save-Button mit Idempotency-Guard (`disabled={!didVendorChange}`) + Toast-Error-Mapping-Response-Pfade (200-changed → success, 200-unchanged → info, 4xx → error, missing_env → warning 8s) + Read-Only Court-List mit `is_active !== false`-Filter
- `app/api/clubs/[id]/hardware-vendor/route.ts` (~200L) — 10-Step-PUT mit `withApiAuth` + `verifyRole(auth, 'admin')` + `checkRateLimitOrFail(STANDARD)` + club-scope-fail-closed-check + Zod-Validate via `z.enum(HARDWARE_VENDORS)` + Read-Before-Update + Idempotency-Short-Circuit → unchanged:true-Response ohne Update+Audit-Log + Shallow-JSONB-Merge `{...existingFeatures, hardware_vendor: newVendor}` + ADR-002 Audit-Log mit `actor_id` (NOT `actor_user_id` per schema) + missing_env-Detection via runtime-process.env-lookup + Non-Blocking-Audit-Insert-Failure (log.warn + continue)
- `tests/unit/app/api/clubs/[id]/hardware-vendor/route.test.ts` (~265L, **14 Cases**) — auth-gate (3×), payload-validation (3×), idempotency (3×), missing-env (2×), error-surfacing (3×); Mock-Pattern mirror zu `tests/unit/lib/hardware/adapter.test.ts`: `vi.mock('@/lib/api-auth')` + `vi.mock('@/lib/supabase/service')` + `vi.mock('@/lib/rate-limit')` + `vi.mock('@/lib/logger')`
- `lib/hardware/adapter.ts` MODIFIED (+7L) — `HARDWARE_VENDORS = ['nuki','shelly','loxone'] as const satisfies readonly HardwareVendor[]` als Single-Source-of-Truth → route.ts nutzt `z.enum(HARDWARE_VENDORS)` statt hardcoded enum, zukünftige Erweiterungen upstream-statt-per-File-Edit

**2. ADR-002 forensic-policy durchgängig**:

- Kein `throw` in User-Facing-Path (alle Errors → status: 4xx/5xx mit detail-Message OR log.warn + continue)
- Audit-Log-Insert mit `actor_id` UUID + `action: 'hardware_vendor_update'` + `resource_type: 'club'` + `details`-JSONB-Snapshot mit `previous_vendor` + `new_vendor` (Audit-Forensic-Trail nachvollziehbar)
- Idempotency: vorher === nachher → unchanged:true ohne Update+Audit-Log (verhindert Audit-Table-Spam + Race-Conditions in der UI)

**3. Test-Coverage großzügig** (14 Cases, 5 describe-blocks):

- Auth + RBAC (3×): 403 member-role, 403 different-club-admin
- Payload-Validation (3×): 400 invalid-vendor, 400 malformed-JSON, 400 missing-field
- Idempotency (3×): unchanged=true, changed-merge, first-time-prev-null
- Missing-Env (2×): NUKI_API_TOKEN unset, SHELLY_API_HOST set
- Error-Surfacing (3×): 500 read-before-fail, 500 update-fail, 200 audit-fail-non-blocking

**4. Code-Reviewer-3-Runden PASS** (R1 + R2 + R3 SHIP-ready):

- R1 (4 actionable Nits): vendor-re-definition → HARDWARE_VENDORS const-array, audit-log actor_id, aria-label-per-radio, idempotency-test-assertion
- R2 (1 re-anchor retry): test-assertion `['clubs','audit_logs']` → `['clubs']` (korrekt beschreibt unchanged-path early-return)
- R3 (2 non-blocking nits): radio-card arrow-key-navigation (WAI-ARIA-Compliance-Optionality), initialVendor-state-sync-für-cross-club-navigation (akzeptabel für single-club-session)
- TSC-baseline 0 errors bestätigt; alle R1-Nits gefixt; R3-nits als Future-Enhancement dokumentiert

**Verifikations-Stand**: TypeScript `tsc --noEmit` → 0 Errors. Code-Reviewer-PASS-R3-SHIP-ready. Vitest-AKZ per dev-machine-gated.

## 1. Wave-Diff vs spec-literal

- Spec: „Adapter-Konfiguration **pro Court**" + „Aufwand 16h"
- Implementation: Club-Level-Vendor (alle Courts eines Clubs teilen denselben Vendor). Per-Court-Override-Struktur (`club_court_vendor_overrides`) als separates 3.1.4-Ticket vorgemerkt.
- Begründung 1.0-Pivot: AKZ „UI funktioniert" wird durch Club-Level-UI erfüllt. Per-Court-Override wäre Tabellen-Migration + 4× API-Endpunkte + E2E-Tests → >16h. Spec-Out-of-Scope erlaubt pragmatische Priorisierung.
- Dokumentiert in diesem File + STATUS.md-Audit-Note Block für Forward-Trace.
