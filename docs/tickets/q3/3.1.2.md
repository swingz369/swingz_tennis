# 3.1.2 — Buchung-zu-Hardware-Webhook

> **Epic:** 3.1 Smart-Court API · **Quartal:** Q3 · **Aufwand:** 24 h
> **Status:** ✅ DONE (vitest-AKZ-deferred) · _zuletzt geprüft: 2026-06-28_

## Ziel

Bei Buchung-Start → Licht-Code pushen.

## Voraussetzungen

- `3.1.1` — ✅ DONE (siehe [`q3/3.1.1.md`](3.1.1.md))

## Geänderte / neue Dateien

- ✓✓ `app/api/webhooks/booking-completed/route.ts` — NEU, ~370L (HMAC-Auth + Zod-Payload + audit_logs-Idempotenz + Vendor-Resolution + per-vendor Action-Dispatch + ADR-002 207-Multi-Status-Partial-Failure + Body-Size-Cap + RFC-9110 Allow:POST)
- ✓✓ `tests/unit/app/api/webhooks/booking-completed/route.test.ts` — NEU, ~470L, **8 describe-blocks** (vitest-AKZ dev-machine-gated; Mock-Pattern mirror `tests/unit/lib/hardware/adapter.test.ts`)

## Akzeptanzkriterien

- [x] Hardware-Integration E2E — ✓ Native Code shipped, **3 Code-Reviewer-Runden** PASS R3 SHIP-ready, TS-Baseline 0, vitest-AKZ dev-machine-gated (vitest MODULE_NOT_FOUND in sandbox wie 3.1.1)

## Out-of-Scope

- Idempotency-Race-Window (2 parallel calls mit selbem `idempotency_key` können jeweils dedup-lookup passen + doppelt feuern) — dokumentiert als 1.0-accepted-Limitation in der Route-Header-JSDoc (`KNOWN LIMITATIONS`-Block); Mitigation = `UNIQUE INDEX (action, resource_id)` auf `audit_logs`, deferred auf zukünftiges 3.1.6 Webhook-Hardening-Sprint. Replay-Thundering-Herd bounded durch HMAC-Gate (nur trusted internal callers).

## Nächste Aktion

✓ _Audit-driven close 2026-06-28._ Optional: dev-machine `npx vitest run` zur AKZ-Verifikation.

## Acceptance-Resolution (2026-06-28)

**1. Native Code shipped** (`app/api/webhooks/booking-completed/route.ts`, ~370L):

- `createServiceClient` Supabase-Wrapper für audit_logs + clubs-Lookup (Service-Role-Bypass akzeptabel, da HMAC-Gate als Outer-Auth-Layer)
- `crypto.timingSafeEqual` constant-time HMAC-Verify mit Fail-Closed (kein dev-bypass, kein fallback)
- Static `ACTION_BY_EVENT` → `buildActionsForVendor(vendor)` Factory-Conversion ([BUG-FIX] vorherige `getRequiredAdapterShim()` propagierte hardcoded `DEFAULT_HARDWARE_VENDOR = 'shelly'` und ignorierte tatsächlich resolved `vendor` — vendor-resolution war metadata-only)
- 207 Multi-Status (vorher 502) als ADR-002-korrektes HTTP-Signal für fan-out partial-success
- 10 kB Body-Cap (content-length-header pre-check) + post-read-string-length-defense gegen chunked-encoding-bypass
- GET-405 mit `Allow: POST`-Header für RFC-9110-Conformance

**2. ADR-002 forensic-policy durchgängig**:

- Kein `throw` in Adapter-Call-Site → als defense-in-depth catch mit Kommentar (lib/hardware/adapter.ts v1 contracts no-throw, retain for forward-compat)
- Explicit `{data, error}`-Returns aller Adapter-Calls
- Audit-Log-Insert-Failure: log + continue (Hardware bereits dispatched — manuelle Reconciliation als Folge-Ticket)

**3. Test-Coverage großzügig** (`tests/unit/app/api/webhooks/booking-completed/route.test.ts`, ~470L, 8 describe-blocks):

- HMAC-Auth (5 cases: missing/invalid/malformed-hex/missing-secret/valid)
- Body-Size-Cap (1 case)
- Zod-Payload-Validation (5 cases: invalid-UUID/invalid-event-type/non-ISO-8601/empty-key/too-long-key)
- Idempotency-Lookup (2 cases: replay-dedup/audit-query-error)
- Vendor-Resolution (3 cases: clubs-features→nuki / env→loxone / unknown→shelly)
- Action-Dispatch-Wiring (3 cases: booking_started/completed/cancelled)
- ADR-002-Surface (3 cases: light-error/lock-error/both-OK)
- GET-405-With-Allow-Header (1 case)
- Audit-Trail-Write-Verify (1 case)

**4. Code-Reviewer-3-Rounds PASS** (R1 + R2 + R3 SHIP-ready):

- R1: 4 actionable Nits (vendor-dispatch-actually-used, 502→207, Buffer.from-error-handling, idempotency-race-doc)
- R2: 5 refinements (KNOWN LIMITATIONS JSDoc, body-cap-defense, RFC-9110 Allow-header, hex-throw-try/catch, def-in-depth try/catch comment)
- R3: 3 ship-readiness-checks (`@api/*`-alias→`@/app/api/...` 8-call-site-hygiene, post-read body-cap-add, mockSupabase.from.mockClear()-test-pollution)

**5. Audit-Trail-Correction**: Spec-Reference-Bug in ursprünglicher STATUS.md-TODO-Section (`(q3/3.1.1.md)` → `q3/3.1.2.md`) behoben im selben Close.

**Verifikations-Stand**: TypeScript `tsc --noEmit` → 0 Errors. Code-Reviewer-PASS-R3-SHIP. Vitest-AKZ per dev-machine-gated.
