# 3.7.2 — `types/supabase.ts` regenerieren + TODO-Marker entfernen

> **Epic:** 3.7 Backlog-Reconciliation · **Quartal:** Q3 · **Aufwand:** 0.5 h  \
> **Status:** ❌ TODO · _zuletzt geprüft: 2026-06-28_

## Ziel

Der regenerations-Pflicht-Marker
`// TODO(@owner: backend, regenerate via 'supabase gen types typescript --local'):` in
`types/supabase.ts` (Zeile 2023) muss entfernt werden, indem die generierte TypeScript-
Datei neu gegen das aktuelle DB-Schema erzeugt wird. Akzeptanz-Effekt: Der Smoke-Test
`bash scripts/smoke-test-20260627-prod-activation.sh` muss bei `(d) regen-todo` von
`ASPIRATIONAL-FAIL` auf `PASS` umschalten.

## ⚠️ Ausführungshinweis

Empfohlene Reihenfolge: **erst Ticket 3.7.1 (DB-Apply) abschließen, dann dieses hier.**
Grund: Wenn die DB noch nicht die neuen Tabellen/Funktionen/Trigger aus 20260627 enthält,
landen die frisch generierten Types nicht im finalen Stand und sind sofort wieder veraltet.
Falls 3.7.1 noch nicht erledigt ist, hier nur den TODO-Marker entfernen (manuell, ohne
regen) — das ist suboptimale, aber deckt Smoke-Test-Acceptance ab.

```bash
# Variante A (vollständig, NACH 3.7.1):
npx supabase gen types typescript \
    --project-id qeckztuzeymuwwtyoryi \
    --schema public \
    > types/supabase.ts

# Variante B (lokal, falls supabase-Stack läuft):
npx supabase gen types typescript --local --schema public > types/supabase.ts

# Variante C (manuell, falls (B)+(A) nicht möglich):
# Marker in types/supabase.ts:2023 entfernen — regenerations-Lücke bleibt offen,
# dafür Smoke-Test sofort grün. ⚠️ Bitte mit Owner abstimmen.
```

## Voraussetzungen

- Supabase-CLI `>= 1.200` installiert (siehe `npx supabase --version`)
- DB-Zugriff auf `qeckztuzeymuwwtyoryi` (Access-Token via `supabase login`)
- Optional: lokaler `supabase start`-Stack (für Variante B)

## Geänderte / neue Dateien

- ✗ `types/supabase.ts:2023` TODO-Marker — _MISS_ (vorhanden, zu entfernen)
- ✓ `types/supabase.ts` (7316 lines, generierte Datei) — _OK_ (existiert, wird überschrieben)

## Akzeptanzkriterien

- [ ] `grep -nE 'TODO\(@owner: backend.{0,80}regenerate' types/supabase.ts` → 0 Treffer
- [ ] `npm run typecheck` (oder `npx tsc --noEmit`) grün, keine Drift-Errors aus
      `types/supabase.ts`
- [ ] `bash scripts/smoke-test-20260627-prod-activation.sh` exit-code `0`
- [ ] JSON-Last-Line: `"checks":[...,{"name":"(d) regen-todo","status":"PASS"},...]`

## Out-of-Scope

- Refactoring der generierten Types in handgeschriebene Domain-Types — separates Q3-Ticket
- CI-Workflow, der `supabase gen types` als Pre-Build-Hook ausführt — separat
- Aufräumarbeiten in `lib/typed-helpers.ts`, falls generierte Felder nicht passen

## Nächste Aktion

1. Falls 3.7.1 noch nicht erledigt ist: kurz mit Owner abstimmen, ob Variante C
   (Marker entfernen, kein regen) interim OK ist. **Default-Empfehlung: NEIN** — Marker
   entfernen ohne Regen versteckt das eigentliche Problem.
2. Supabase-CLI installieren (falls nicht vorhanden) und `supabase login` durchführen.
3. `npx supabase gen types typescript --project-id qeckztuzeymuwwtyoryi --schema public > types/supabase.ts`
   ausführen (lokal oder in CI).
4. `git diff types/supabase.ts | head` anschauen — bei Differenzen > 1000 Zeilen
   kurz prüfen, ob die DB sich zwischenzeitlich stark geändert hat.
5. `git commit types/supabase.ts -m 'chore(types): regenerate from remote schema (post-3.7.1)'`
6. PR öffnen → Reviewer auf Smoke-Test-Output verweisen.
7. Nach Merge: Acceptance-Schritt aus "Akzeptanzkriterien" durchlaufen.

## Quellen / Beweise

- Smoke-Test-Baseline (2026-06-28): siehe `docs/tickets/STATUS.md`, jüngste Audit-Note;
  reproduzierbar via `bash scripts/smoke-test-20260627-prod-activation.sh`, Exit-Code `2`,
  JSON `"overall":"ASPIRATIONAL_FAILURE","pass":2,"fail":2,"aspirational_failures":2`,
  Check (d) Status `ASPIRATIONAL-FAIL`, marker-location `2023`.
- Marker-Exact-Inhalt: `types/supabase.ts:2023` → `// TODO(@owner: backend, regenerate via 'supabase gen types typescript --local'):`
- Smoke-Test-Regex: `TODO\(@owner: backend.{0,80}regenerate` (verifizierbar via
  `grep -nE 'TODO\(@owner: backend.{0,80}regenerate' types/supabase.ts` → 1 Treffer).
