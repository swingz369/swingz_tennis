# 3.7.1 — DB-Migrationen anwenden (Matches-Tabelle & ELO-Rating-System)

> **Epic:** 3.7 Backlog-Reconciliation · **Quartal:** Q3 · **Aufwand:** 2 h  \
> **Status:** ❌ TODO · _zuletzt geprüft: 2026-06-28_

## Ziel

Den angeblichen 2026-06-27-prod-activation-Block endlich auf der Live-DB materialisieren:
Die Migrationen `20260627_season_group_weeks.sql` und `20260627_elo_rating_system.sql`
sollen tatsächlich appliziert werden, gemeinsam mit der fehlenden `public.matches`-Tabelle,
auf der das ELO-Trigger `trg_match_elo_update` sitzt.

Akzeptanz-Effekt: Der Smoke-Test `bash scripts/smoke-test-20260627-prod-activation.sh`
muss nach dem Apply bei `(c) elo-trigger` von `ASPIRATIONAL-FAIL` auf `PASS` umschalten.
Im Anschluss an diesen Apply läuft Ticket `3.7.2` (types/supabase.ts regenerieren +
TODO-Marker entfernen), um den Smoke-Test auf den finalen `exit_code: 0`-Zustand zu
bringen — die beiden Tickets gehören zusammen, Reihenfolge: `3.7.1` → `3.7.2`.

## ⚠️ Ausführungshinweis (Pflicht)

**`npx supabase db push` MUSS manuell auf einer Entwickler-Maschine ausgeführt werden —
NICHT aus der Codebuff-Sandbox heraus.** Die Sandbox hat keine ausgehende Verbindung zum
Supabase-Projekt `qeckztuzeymuwwtyoryi` (Pooler-Host), und ein versehentlich gestarteter
`db push` aus der Sandbox könnte den Migrations-Tracker beschädigen.

Konkreter Ablauf:

```bash
# 1. Auf dev-Maschine klonen, branch ausschecken:
git clone git@github.com:aeugeln/SwingZ.git
cd SwingZ
git checkout -b chore/apply-20260627-elo-batch

# 2. (siehe "Geänderte / neue Dateien" unten — neue Migrationen anlegen)
#    Elo-Migration aus orphaned commit wiederherstellen
git show 9220e43749d893fc673b5859770e6084991a0bbe:supabase/migrations/20260627_elo_rating_system.sql \
  > supabase/migrations/20260627_elo_rating_system.sql

# 3. supabaseStack lokal starten
npx supabase start

# 4. Lokal prüfen, dass die Migrationen trocken durchlaufen
npx supabase db reset --no-seed    # frischt lokale DB komplett auf; alle 20260627_* in lex-order

# 5. Auf remote anwenden
npx supabase link --project-ref qeckztuzeymuwwtyoryi
npx supabase db push

# 6. Akzeptanz-Test
bash scripts/smoke-test-20260627-prod-activation.sh
# Erwartet: exit-code 0, JSON payload "overall":"PASS","pass":4,"fail":0
```

## Apply-Order (lexikografisch sortiert ist Pflicht)

Supabase `db push` sortiert Migrations-Files alphabetisch. Daher MUSS die Datei für die
`matches`-Tabelle lexikografisch VOR `20260627_elo_rating_system.sql` stehen:

| Reihenfolge | Datei | Wirkung |
|---|---|---|
| 1 | `20260627_create_matches_table.sql` (NEU) | erstellt `public.matches` + RLS + Indizes |
| 2 | `20260627_elo_rating_system.sql` (RECOVERED) | fügt `users.elo_rating`, `elo_history`, `compute_elo_delta`, `trg_match_elo_update` |
| 3 | `20260627_season_group_weeks.sql` (EXISTING) | erstellt `season_group_weeks` |

**Warum `matches` zwingend zuerst?** Die recoverable ELO-Migration (siehe
`/tmp/elo_migration_recovered.sql`, Zeile 91 `REFERENCES public.matches(id)` und Trigger
`AFTER INSERT OR UPDATE ON public.matches`) referenziert die Tabelle, ohne sie zu erzeugen.
Ein direkter Apply ohne vorherige `matches`-Tabelle würde mit Fehler
`relation "public.matches" does not exist` abbrechen.

## Voraussetzungen

- Lokal läuft `supabase start` (Docker-Stack) sauber durch
- `clouds`/`projects`-Zugriff auf `qeckztuzeymuwwtyoryi` (Pooler-Role: `postgres`)
- Git-Tag `9220e43` ist erreichbar aus dem Working-Tree (für die Elo-Recovery)

## Aufwandsschätzung (Forward-Breakdown)

Gesamt: **2 h** (Aufwand-Score). Per-Step-Percent-Breakdown:

| # | Step                                                                                       | Zeit   | Aufwand-Anteil |
| - | ------------------------------------------------------------------------------------------ | ------ | -------------- |
| 1 | Skelett-SQL für `20260627_create_matches_table.sql` ausformulieren + Spalten-Shape-Abgleich gegen `/tmp/elo_migration_recovered.sql:6-150` | 0.5 h  | 25 % |
| 2 | Elo-Migration aus orphaned Commit `9220e43` wiederherstellen via `git show`                | 0.1 h  | 5 %  |
| 3 | Lokal mit `npx supabase db reset --no-seed` validieren (alle 20260627_* in lex-order)      | 0.3 h  | 15 % |
| 4 | PR öffnen + Reviewer auf Smoke-Test-Output verlinken                                       | 0.2 h  | 10 % |
| 5 | Nach Merge: Remote `npx supabase db push --project-ref qeckztuzeymuwwtyoryi`                | 0.4 h  | 20 % |
| 6 | Smoke-Test-Validation: `bash scripts/smoke-test-20260627-prod-activation.sh` (AKZ-Mapping siehe unten) | 0.3 h  | 15 % |
| 7 | Kontrolle der 4 SQL-Assertions (PostgREST COUNT-Checks) + zusätzlicher Reviewer-Gate-Approval | 0.2 h | 10 % |
| **Σ** | **(Forward-Plan)**                                                                                          | **2.0 h** | **100 %** |

> **Hinweis Aufwand-Confidence**: Estimated ggf. abweichend bei unerwarteten Schema-Konflikten
> zwischen `elo_history` und `public.matches` (z. B. `REFERENCES public.matches(id)` zielt auf
> Spalte die nicht genau in der Skelett-Definition steht). Realisierter Aufwand wird im
> `Acceptance-Resolution`-Block bei Close vermerkt.

## Geänderte / neue Dateien

- ✗ `supabase/migrations/20260627_create_matches_table.sql` — _MISS_ (neu erstellen, siehe Skelett unten)
- ✗ `supabase/migrations/20260627_elo_rating_system.sql` — _MISS_ (orphaned commit `9220e43`, wiederherstellen via `git show` wie oben)
- ✓ `supabase/migrations/20260627_season_group_weeks.sql` — _OK_ (397 LOC, in HEAD)

### ⚠️ Skelett für `20260627_create_matches_table.sql` — DO NOT APPLY AS-IS

> **WARNUNG**: Das untenstehende Skelett ist eine **Orientierung**, kein
> produktionsreifer Code. Vor dem Authoring MUSS der Spalten-Shape gegen
> `/tmp/elo_migration_recovered.sql` Zeilen 6–150 exakt abgeglichen werden:
>
> - `compute_elo_delta(input1 elo_rating, input2 elo_rating, score1 int, score2 int)`
>   erwartet pro Seite eine Score-Summe und nutzt Per-Side-Average über
>   `side_left`/`side_right` (Doubles → Tournament-Modus).
> - FK-Constraints in `elo_history` (z. B. `match_id uuid NOT NULL REFERENCES
>   public.matches(id) ON DELETE CASCADE`) müssen zu Spalten in dieser Tabelle passen.
> - RLS-Policies dieser Tabelle müssen mit Trainer-/Admin-Rollen
>   konsistent sein (siehe `2.2.1` für RBAC-Pattern).
>
> **Reviewer-Gate**: Vor `npx supabase db push` MUSS ein zweiter Maintainer
> das ausformulierte SQL gegen `/tmp/elo_migration_recovered.sql` abgenommen haben.

```sql
-- 20260627_create_matches_table.sql
-- Schema der Matches-Tabelle, auf die 20260627_elo_rating_system.sql
-- via FK + Trigger referenziert. Felder incl. RLS werden mit der
-- bestehenden match_results-Tabelle abgeglichen.

CREATE TABLE IF NOT EXISTS public.matches (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  club_id       uuid NOT NULL REFERENCES public.clubs(id) ON DELETE CASCADE,
  group_id      uuid NOT NULL REFERENCES public.groups(id) ON DELETE CASCADE,
  created_at    timestamptz NOT NULL DEFAULT now(),
  played_at     timestamptz NOT NULL,
  side_left     jsonb NOT NULL,                  -- {player_ids: [...], score: int}
  side_right    jsonb NOT NULL,
  winner_side   text CHECK (winner_side IN ('left','right','draw')) NOT NULL,
  is_official   boolean NOT NULL DEFAULT false,   -- nur OFFICIAL matches triggern ELO
  updated_at    timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_matches_club_id      ON public.matches(club_id);
CREATE INDEX IF NOT EXISTS idx_matches_played_at    ON public.matches(played_at);
CREATE INDEX IF NOT EXISTS idx_matches_is_official  ON public.matches(is_official)
  WHERE is_official = true;

ALTER TABLE public.matches ENABLE ROW LEVEL SECURITY;
-- Club-Mitglieder dürfen lesen; Trainer/Admins dürfen schreiben
-- (exakte Policies mit Billing-/Trainer-RBAC synchronisieren — Details TODO in 3.7.1)
```

> **Hinweis**: Spalten `side_left`/`side_right` (Doubles-Pair-Average laut
> `/tmp/elo_migration_recovered.sql` Zeile 6-8) müssen die Struktur haben, die die
> Elo-Berechnung in `compute_elo_delta` erwartet. Vor dem Authoring: das Original-SQL
> Zeile 6-150 noch einmal durchgehen und Spalten-Shape abgleichen.

## Akzeptanzkriterien

> **Akzeptanz ↔ Smoke-Test-Mapping**: Jeder AKZ-Bullet ist einem konkreten Check im
> `scripts/smoke-test-20260627-prod-activation.sh` (oder einer unabhängigen DB-Assertion
> für den initialen Schema-State) zugeordnet. Bei Close wird im `Acceptance-Resolution`-Block
> dokumentiert welcher AKZ tatsächlich PASS erreicht hat.

| # | AKZ-Verifikation                                                                                              | Smoke-Test-Check-ID / Defacto-Assertion | Erwarteter Wert |
| - | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | --------------- |
| 1 | `npx supabase db push` läuft auf dev-Maschine ohne Fehler durch                                                | (a) `db-push-exit-success` _(forward-projected)_ (implizit über Folgechecks) | exit-0 (sub-step) |
| 2 | `SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public' AND table_name='matches'` → `1` | (b) `matches-table-exists` _(forward-projected)_ | rowcount=1             |
| 3 | `SELECT COUNT(*) FROM pg_proc WHERE proname IN ('compute_elo_delta','match_elo_update_trigger')` → `2`         | (c-1) `elo-functions-exist` _(forward-projected)_ | rowcount=2             |
| 4 | `SELECT COUNT(*) FROM pg_trigger WHERE tgname = 'trg_match_elo_update'` → `1`                                  | (c-2) `elo-trigger-exists` _(forward-projected, Sub-Check von c-1)_ | rowcount=1             |
| 5 | `bash scripts/smoke-test-20260627-prod-activation.sh` exit-code `0`                                            | `(c-3) elo-trigger` _(forward-projected Sub-Check, final integration)_ | exit-0                 |
| 6 | JSON-Last-Line der Smoke-Test-Ausgabe enthält `"overall":"PASS","pass":4,"fail":0,"aspirational_failures":0,"exit_code":0` | (final-json) `overall-state` _(forward-projected)_ | string-match            |

> **Forward-Projected-IDs-Klausel**: Die mit `_forward-projected_` markierten Check-IDs sind
> **dokumentierte Vorab-Spec-Akz-Benennungen für die in 3.7.1/3.7.2 geplanten DB-Wirkungen**.
> Bei Close-Ticket-Zeitpunkt MUSS via
> `grep -nE '\([a-z]+(-[a-z0-9]+)?\)|\(final-[a-z]+\)' scripts/smoke-test-20260627-prod-activation.sh`
> verifiziert werden, ob die Smoke-Test-Subchecks tatsächlich angelegt wurden. Wenn nicht,
> muss der Smoke-Test im Rahmen dieses Tickets oder 3.7.2 ergänzt werden, bevor auf
> `Realisiert: 2 h`-Status gewechselt wird. Aktuell dokumentiert der
> `scripts/smoke-test-20260627-prod-activation.sh` nur Check-Namen
> `(a)`/`(b)`/`(c) elo-trigger`/`(d) regen-todo` (siehe `STATUS.md` Audit-Note 2026-06-28);
> die `(c-1)`/`(c-2)`/`(c-3)`/`(final-json)`-Sub-Breaks sind Forward-Spec-Marker, die Close
> nachgezogen werden müssen.

- [ ] AKZ-1: `npx supabase db push` läuft auf dev-Maschine ohne Fehler durch
- [ ] AKZ-2: `SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public' AND table_name='matches'` → `1`
- [ ] AKZ-3: `SELECT COUNT(*) FROM pg_proc WHERE proname IN ('compute_elo_delta','match_elo_update_trigger')` → `2`
- [ ] AKZ-4: `SELECT COUNT(*) FROM pg_trigger WHERE tgname = 'trg_match_elo_update'` → `1`
- [ ] AKZ-5: `bash scripts/smoke-test-20260627-prod-activation.sh` exit-code `0`
- [ ] AKZ-6: JSON-Last-Line der Smoke-Test-Ausgabe enthält `"overall":"PASS","pass":4,"fail":0,"aspirational_failures":0,"exit_code":0` (Forward-Operator-Hinweis: aktuell `"exit_code":2` → Ziel `"exit_code":0`)

## Out-of-Scope

- Frontend-Use-Cases für `elo_rating` (UI-Anzeige, Leaderboard) — separates Q3-Ticket
- Migration von `match_results` (legacy-Schema, Q2-Tickets) zu `matches` (canonical) — separates Q3-Ticket
- Performance-Tuning des Triggers (`EXPLAIN ANALYZE`)

## Nächste Aktion

1. Auf dev-Maschine: Skelett-SQL aus "Geänderte / neue Dateien" zu
   `20260627_create_matches_table.sql` ausformulieren (Spalten-Shape exakt nach
   `/tmp/elo_migration_recovered.sql` Zeilen 6-150 ausrichten).
2. Elo-Migration aus `git show 9220e43:...` wiederherstellen.
3. Lokal mit `npx supabase db reset --no-seed` testen.
4. PR öffnen, Reviewer auf Smoke-Test-Output verlinken.
5. Nach Merge: `npx supabase db push` auf remote.
6. Acceptance: Smoke-Test-Script muss `PASS` melden.

## Quellen / Beweise

- Smoke-Test-Baseline (2026-06-28): siehe Audit-Trail im Repo-STATUS (`docs/tickets/STATUS.md`,
  jüngste Audit-Note); reproduzierbar via `bash scripts/smoke-test-20260627-prod-activation.sh`,
  Exit-Code `2`, JSON `"overall":"ASPIRATIONAL_FAILURE","pass":2,"fail":2,"aspirational_failures":2`.
- Verwandtes Original-Ticket: `docs/tickets/q2/2.2.1.md` (TICKET-2.2.1, ELO-System,
  Status `❌ TODO`, Aufwand 16 h — durch diesen Apply voraussichtlich teilweise schließbar).
- Recoverable Migration-Inhalt: `/tmp/elo_migration_recovered.sql` (aus Commit
  `9220e43749d893fc673b5859770e6084991a0bbe`).
