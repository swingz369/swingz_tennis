# 3.7.4 — `training_groups` final droppen + ADR-013 Source-of-Truth-Deklaration

> **Epic:** 3.7 Backlog-Reconciliation · **Quartal:** Q3 · **Aufwand:** 2 h (mittelfristig, user-stated)  \
> **Status:** ❌ TODO · _zuletzt geprüft: 2026-07-01_

## ⚠️ Numberingkonflikt zur User-Anfrage

Der User-Auftrag lautete wörtlich "Ticket 3.7.3 anlegen". Da `docs/tickets/q3/3.7.3.md` jedoch bereits in diesem Quartal durch den `season_planning_history.action_type`-CHECK-Whitelist-Mismatch (23514 nach FK-Fix) belegt ist, wird dieses Ticket unter der nächsten freien Nummer **3.7.4** geführt — die Inhalte sind **neu** und nicht auf das bestehende 3.7.3-Ticket bezogen. Sollte der User explizit 3.7.3-Renaming wünschen, bitte nachholen.

## Ziel

Zwei eng gekoppelte Aufräumarbeiten, die die Schatten-Architektur zwischen `groups` (moderne Cluster-Truth) und `training_groups` (Legacy, leer) strukturell auflösen:

1. **`training_groups` final droppen** — die Tabelle hat **0 Rows** in Live-DB (Audit-Snapshot vom 2026-07-01, siehe 3.7.3-Quellen-Spalte "Live-Row-Counts"). Sie blockiert weiterhin Drizzle-Generates via FK-Constraints und ist nach Migration `20260630_recorrect_...sql` (FK zeigt jetzt auf `groups(id)`) **funktionslos**. Restlos entsorgen.
2. **ADR-013 schreiben** — Source-of-Truth-Deklaration. Konsolidiert die Lehren aus 3.7.1 / 3.7.2 / 3.7.3 / FK-Drift-Fix in eine zentrale Architektur-Entscheidung: **keine Schatten-Tabellen**, **explizite FK-Invariants**, **ein Trigger pro Audit-Tabelle**, **Drizzle als Cache, nicht als Authorität**.

## ⚠️ Ausführungshinweis

Wie bei 3.7.3 müssen Migration-Apply + ADR-Commit manuell auf einer Entwickler-Maschine erfolgen.

```bash
# Auf dev-Maschine:
git checkout -b chore/3.7.4-training-groups-drop-source-of-truth-adr
# 1. ADR-013 Inhalt finalisieren (siehe ADR-Outline unten) und committen
# 2. Migration-SQL:  supabase/migrations/20260702_drop_training_groups_table.sql
#    Sequence:  BEGIN; DROP CONSTRAINTS (alle FK-references auf training_groups); DROP TABLE; COMMIT;
# 3. Drizzle-Schema: src/infrastructure/persistence/schema.ts — `trainingGroups` export entfernen
# 4. Drizzle:      npm run drizzle -- generate ; npm run drizzle -- migrate
# 5. Vitest:       npx vitest run tests/unit/lib/season-planning --reporter=verbose
# 6. PR + Reviewer
```

## Symptom / Begründung "warum droppen"

| Quelle | Wert | Bedeutung |
|---|---|---|
| Live-Audit 2026-07-01 | 0 Rows in `training_groups` | Reine Phantom-Tabelle |
| FK-Drift-Historie Commit `fdd1ce0` (2026-05-07) | `season_plan_entries.group_id` zeigt(e) auf `training_groups` | Ursprung des 23503-Bugs |
| FK-Korrektur 2026-06-30 | FK zeigt jetzt auf `groups(id)` | Tabelle ist funktionslos |
| Drizzle-Snapshot `0009`/`0010` | referenziert `training_groups` noch in FK-Generierung | Drizzle-Cache stale |

## Geänderte / neue Dateien

- ✗ `supabase/migrations/20260702_drop_training_groups_table.sql` (NEU) — DROP-CONSTRAINT-Block, dann `DROP TABLE IF EXISTS training_groups CASCADE`
- ✗ `drizzle/00XX_drop_training_groups.sql` (auto-generated vom Drizzle-Run)
- ✗ `src/infrastructure/persistence/schema.ts` — `export const trainingGroups = …` entfernen
- ✗ `drizzle/meta/00YY_snapshot.json` (auto-updated)
- ✗ `docs/ADR-013-source-of-truth-declaration.md` (NEU, siehe ADR-Outline unten)
- ✗ `docs/STATUS.md` — 3.7.4 als ✅ DONE markieren nach Apply

## Akzeptanzkriterien

| # | AKZ-Verifikation | Erwarteter Wert |
| - | --- | --- |
| 1 | `psql -c "SELECT count(*) FROM training_groups"` **vor** Apply | 0 |
| 2 | Nach Apply: gleiche Query | `relation "training_groups" does not exist` |
| 3 | `npm run drizzle -- generate` zeigt KEINEN Schema-Drift | exit 0, keine pending changes |
| 4 | ADR-013 existiert in `docs/` mit Standard-Sections (Context, Decision, Consequences, Compliance) | Datei committed + sections vorhanden |
| 5 | Keine Regression: `lib/season-planning/clustering-engine.ts` läuft weiterhin, schreibt nach `groups` | `npx vitest run tests/unit/lib/season-planning` exit 0 |
| 6 | Keine FK-Constraint-Verletzungen mehr auf `training_groups` nach Apply | `pg_dump --schema-only` zeigt keine depend-FKs auf `training_groups` |

- [ ] AKZ-1: 0 Rows bestätigt (vor Drop)
- [ ] AKZ-2: DROP erfolgreich
- [ ] AKZ-3: drizzle-kit diff clean
- [ ] AKZ-4: ADR-013 committed
- [ ] AKZ-5: Cluster-Engine Vitest grün
- [ ] AKZ-6: pg_dump zeigt keine depend-FKs

## ADR-013 Outline (zu erstellender Inhalt)

Siehe `docs/ADR-013-source-of-truth-declaration.md` (parallel erstellt). Kerninhalte:

1. **Context** — drei unabhängige Bugs (FK-Drift 23503 + CHECK 23514 + Trainer-Profile-Drift) innerhalb 3 Wochen, alle durch Schema-Drift zwischen Source-of-Truth-Texten.
2. **Decision** — 4 Prinzipien: 1-Tabelle-pro-Konzept | FK-Invariant-Kommentare | 1-Trigger-pro-Audit-Tabelle | Drizzle ist generativ, ADR + Live-DB ist autoritativ.
3. **Consequences** — Pro: keine 23503/23514-Re-Trigger | Contra: Konsistenz-Aufwand pro Schatten-Paar (~30 min), ADR-Wartung.
4. **Compliance** — CI-Gate `scripts/ci-check-shadow-tables.ts` (separates Ticket) greppt `pg_class` auf bekannte Schatten-Paare, blockt PR.
5. **Alternatives Considered** — A (Status quo) ❌, B (Big-Bang) ❌, C (Inkrementell) ✅.

## Out-of-Scope

- **Andere Schatten-Paare** (`trainers`↔`trainer_profiles`, `bookings`↔`training_sessions`, `memberships` Legacy): separate Folge-Tickets. ADR-013 trackt sie als known-debt.
- **Trigger-Refactoring** zu einer `log_season_planning_event()`: 3.7.3-Folge B (separat priorisiert).
- **CI-Gate-Implementierung** `scripts/ci-check-shadow-tables.ts`: weiteres Folge-Ticket (ADR-013-konform, eigene Aufwandsschätzung).
- **Live-Row-Count-Audit** für die anderen Schatten-Paare: bereits in 3.7.3-Quellen gemacht (siehe Audit-Tabelle), keine Wiederholung nötig.

## Aufwandsschätzung (Forward-Breakdown)

Gesamt: **2.0 h** (Aufwand-Score, user-stated mittelfristig).

| # | Step | Zeit | Aufwand-Anteil |
| - | --- | --- | --- |
| 1 | ADR-013 Inhalt entwerfen + Reviewer-Discuss mit Maintainer | 0.50 h | 25 % |
| 2 | Migration-SQL: DROP CONSTRAINTs (alle FK-references auf `training_groups`) + DROP TABLE + IF EXISTS guards | 0.40 h | 20 % |
| 3 | Drizzle-Schema-Cleanup: `trainingGroups` Export löschen, regenerate, diff verifizieren | 0.30 h | 15 % |
| 4 | Live-Apply auf dev-DB + Repro-Verifizierung (Cluster-Engine grün) | 0.40 h | 20 % |
| 5 | PR-Vorbereitung + STATUS.md DONE-mark + Reviewer | 0.40 h | 20 % |
| **Σ** | (Forward-Plan) | **2.0 h** | **100 %** |

> **Empfehlung Reihenfolge**: Schritt 1 (ADR-013) **vor** Schritt 2 (Migration). Begründung: ADR ist die Rechtfertigung für die Migration — sie sollte zuerst als "Proposed" sichtbar sein, damit der Drop nicht als ad-hoc-Firefighting missinterpretiert wird. Status in ADR wandert im selben PR von Proposed → Accepted.

## Quellen / Beweise

- **3.7.3 Audit-Tabelle** (Live-Row-Counts 2026-07-01): `training_groups` = **0 Rows**, `groups` = 31 Rows. (Siehe 3.7.3-Quellen-Abschnitt.)
- **FK-Drift-Git-Beweis** in `supabase/migrations/20260507000000_schema_consolidation.sql` Commit `fdd1ce0` (siehe 3.7.3-Quellen + Read-Code-Block).
- **Drizzle-Snapshot-Beweis**: `drizzle/meta/0009_snapshot.json` enthält noch FK auf `training_groups`, was die Stale-Cache-Hypothese untermauert.
- **Migration-Reihenfolge-Sicherheit**: `20260630_recorrect_...` (FK-Fix) → `20260702_drop_training_groups.sql` (dieses Ticket) ist sicher, weil FK bereits auf `groups(id)` zeigt — `training_groups` kann ohne FK-Violation gedroppt werden.

## Verwandte Tickets

- **3.7.3** — `season_planning_history.action_type` CHECK-Whitelist-Mismatch (23514 nach FK-Fix). Behebt parallel-bug-auf-selber-tabelle-`season_plan_entries`-Channel.
- **3.7.1, 3.7.2** — Smart-Court-Activation / Type-Regen.
- **FK-Drift-Fix** Migration `20260630_recorrect_season_plan_entries_group_fk.sql` — maskiert die `training_groups`-Drift als akute Gefahr; nach FK-Fix ist die Tabelle entbehrlich.

## Nächste Aktion

1. ADR-013-Entwurf in `docs/ADR-013-source-of-truth-declaration.md` finalisieren (parallel zu diesem Ticket angelegt; Status 🟡 Proposed).
2. Auf dev-Maschine `git checkout -b chore/3.7.4-training-groups-drop-source-of-truth-adr`.
3. Migration-SQL `supabase/migrations/20260702_drop_training_groups_table.sql` schreiben.
4. Drizzle-Schema-Cleanup (`src/infrastructure/persistence/schema.ts`).
5. Live-Apply (`npm run drizzle -- migrate`).
6. Vitest-Cross-Check (`npx vitest run tests/unit/lib/season-planning`).
7. PR öffnen → REVIEWER-AKZ-Mapping in Commit-Message.
8. ADR-013-Status im selben PR auf "Accepted" setzen.
9. STATUS.md 3.7.4 auf ✅ DONE.
