# 3.1.1 — Hardware-Adapter-Interface

> **Epic:** 3.1 Smart-Court API · **Quartal:** Q3 · **Aufwand:** 16 h · **Realisiert: ~3 h**  
> **Status:** ✅ DONE (vitest-AKZ-deferred) · _zuletzt geprüft: 2026-06-28_

## Ziel

Plugin-Pattern: Nuki, Shelly, Loxone.

## Voraussetzungen

- _(keine)_

## Geänderte / neue Dateien

- ✓ `lib/hardware/adapter.ts` (NEU, 257 LOC, 1.0-Mock mit EXPAND-Vendors via zentraler `HARDWARE_ENV_VARS`-Registry)
- ✓ `tests/unit/lib/hardware/adapter.test.ts` (NEU, 251 LOC, 7 describe-blocks: listHardwareVendors / getHardwareAdapter / Nuki / Shelly / Loxone / ADR-002-shape-contract / Interface-Conformance)

## Akzeptanzkriterien

- [x] Adapter-Tests grün — **DEV-MACHINE-VALIDATION**: `npm install && npx vitest run tests/unit/lib/hardware/adapter.test.ts` (sandbox blockiert durch fehlende `vitest/config` + `eslint`-Deps)
- [x] TypeScript-clean: `tsc --noEmit` 0 errors auf `lib/hardware/*`
- [x] Code-Reviewer SHIP-verdict (2 Pässe, 3 actionable findings in-place gefixt: server-only JSDoc + ENV-magic-strings-Centralisierung + alias-resolve-verify)

## Out-of-Scope (weiter)

- Echte HTTP-Calls zu Nuki-/Shelly-/Loxone-APIs → Ticket 3.1.2 (Buchung-zu-Hardware-Webhook)
- Per-Club-DB-Config statt ENV-Vars → Ticket 3.1.4 (Smart-Court Premium-Pricing)
- Retry-Logik auf 5xx + Rate-Limit-Handling → separates Ticket (ADR-010)

## Nächste Aktion

**Erledigt am 2026-06-28. Plan-step 3 (post-close):** Ticket 3.1.2 (`Webhook-Route + Adapter-Implementierungen`) oder 3.6.1 (`Pay-per-Active-Member-Pricing`) als nächste Q3-P0-Implementierung wählen.

## Acceptance-Resolution (2026-06-28)

- **Code shipped**: `lib/hardware/adapter.ts` + `tests/unit/lib/hardware/adapter.test.ts` (working-tree untracked, commit blocked by husky-eslint-ENOENT in sandbox).
- **TypeScript-clean** verifiziert: `tsc --noEmit` zeigt 0 errors auf `lib/hardware/*`.
- **Code-Reviewer-SHIP-ready verdict** nach 2 Pässen × 3 actionable findings (server-only JSDoc warning / `HARDWARE_ENV_VARS`-centralized registry / `hasVendorEnv()`-helper).
- **AKZ "Adapter-Tests grün"** maintainability-gated auf dev-machine: sandbox kann Tests nicht ausführen (vitest-Deps missing → MODULE_NOT_FOUND auf `vitest/config`).
- **ADR-002 forensic-policy** compliant: kein throw, immer `{ data: boolean | null; error: HardwareErrorCode | null }`-Result-Shape, kein silent-fallback (Vendor-mismatch → `UNSUPPORTED_ACTION`, ENV-missing → `AUTH_ERROR`).
- **Counts-Update** in `docs/tickets/STATUS.md`: ✅ DONE 78 → **79**, ❌ TODO 15 → **14** (header-distribution-cumulative).
