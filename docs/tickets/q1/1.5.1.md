# 1.5.1 — Composite-Key für `supabase_migrations.schema_migrations`

> **Epic:** 1.5 Schema-Housekeeping · **Quartal:** Q1 · **Aufwand:** 30 min  
> **Status:** ✅ DONE (ADR-007 ACCEPTED — Status-Quo belassen, Forward-Ticket geschlossen) · _zuletzt geprüft: 2026-06-28_  
> **Weiter:** [ADR-007](../decisions/adr-007-schema-migrations-composite-pk.md) ✅ ACCEPTED per Commit `f87988f`. Migration-Sketch bleibt ready-to-apply für Re-Evaluation-Trigger (siehe ADR-007 § Konsequenzen-Sektion).

## Kontext

Forward-Ticket, eröffnet nach Q1-Housekeeping-Audit
(`scripts/bulk-track-only-migrations.sh` Lauf 2026-06-27). Hintergrund: das
Supabase-Schema hat `schema_migrations.version` als **PRIMARY KEY** — multi-
Files-per-Day (z. B. 5 Files `20260506_*.sql`) repräsentieren eine einzige
logische Migration an diesem Datum. ON-CONFLICT-Skip dedupliziert auf
Schema-Level, was für den aktuellen Use-Case (Drift-Apply + Bulk-Track)
**korrekt und ausreichend** ist. Ergebnis: Pending 80 → Inserted 25, ON-
CONFLICT-Skip 55 (24 pre-existing-Keys + 31 in-batch-duplicate-Version-Keys),
Schema-State 49.

## Ziel

Entscheidung herbeiführen: **Composite-Key `(version, name)` einführen ODER
Status quo belassen**. Trade-off-Doc + Migration-Sketch für den Fall der
Aktivierung.

## Akzeptanzkriterien

- [x] **Trade-off abgewogen**: Pro-Composite (Per-File-Granularität für
      Audit-Trail, mehrere additive Migrations pro Tag werden separat
      trackbar) vs. Pro-Status-quo (Simpler Join-Key, kein Schema-Bruch,
      Drift-Apply bleibt im aktuellen Pattern). **Ergebnis**: Status-Quo
      belassen, 5-Punkte-Begründung in
      [ADR-007 § Begründung](../decisions/adr-007-schema-migrations-composite-pk.md)
      dokumentiert.
- [x] **Migration-Sketch reviewed**: ready-to-apply SQL-DDL + Script-Diff
      ist im ADR-007 § Migration-Sketch verankert; Aktivierung nur falls
      Audit-Trail-Anforderung steigt.
- [x] **Doku aktualisiert**: ADR-007 als Schwester zu ADR-001/002/003 ist
      finalisiert (Commit `b8f3da8`) und Maintainer-accepted
      (Commit `f87988f`).
- [x] **Maintainer-Signoff erhalten**: ADR-007 wurde am 2026-06-28
      Maintainer-signed-off → Status 📋 PROPOSED → ✅ ACCEPTED per Commit
      `f87988f`. Forward-Ticket damit auflösbar.

## Acceptance-Resolution (2026-06-28 — Commit `f87988f`)

Dieses Forward-Ticket ist mit dem Maintainer-Signoff für ADR-007
abgeschlossen. Der Status-Quo (PK `(version)` Single-Column) ist bestätigt:

- **ADR-007 ACCEPTED** (Commit `f87988f`): Status 📋 PROPOSED → ✅ ACCEPTED.
  Decision-Date: 2026-06-28. Maintainer-Signoff received.
- **Migration-Sketch bleibt ready-to-apply** für die 4 Re-Evaluation-Trigger
  in [ADR-007 § Konsequenzen](../decisions/adr-007-schema-migrations-composite-pk.md).
- **Schema-Migration wurde NICHT ausgeführt**: PK `(version)` bleibt
  Single-Column. Kein SQL-Lauf, kein DDL-Strukturbruch-Risiko.
- **`scripts/bulk-track-only-migrations.sh` bleibt unverändert**:
  `ON CONFLICT (version) DO NOTHING`.
- **Audit-Trail**: vollständig dokumentiert in
  `docs/tickets/STATUS.md` § "Heute closed / finalized (2026-06-28)"
  inkl. Net-Bilanz-Tabelle + ADR-007 § Changelog.

## Re-Evaluation-Trigger (von ADR-007 übernommen)

Wenn einer der folgenden Trigger aktiviert wird, ist ADR-007 neu zu bewerten
UND ggf. der PK-Wechsel durchzuführen — in diesem Fall wird dieses Ticket
oder ein Folge-Ticket reaktiviert:

1. **Audit-Trail-Spezifikation**: Stakeholder verlangen explizit, welche
   der `20260506_*`-Dateien welcher pre-existing-Version entsprechen.
2. **Migrations-Granularität > 1/Day systematisch**: Mehrere additive
   Migrationen pro Tag regelmäßig (statt 1-File-pro-Day mit Sub-Steps).
3. **DB-Constraint-Conflict-Risiko**: Zukünftige Supabase-Updates
   beeinträchtigen DROP+ADD PK auf managed-Tabelle.
4. **Maintenance-Last bei Composite-PK**: Custom-Logic im
   `bulk-track-only-migrations.sh` problematisch (z. B.
   in-batch-duplicate-Detection).

## Out-of-Scope (zwar historisch als Forward-Ticket, jetzt aber explizit abgeschlossen)

- ~~Schema-Migration ausführen~~ (= bleibt Status quo bis Maintainer-
  Entscheidung → ENTSCHIEDEN am 2026-06-28: Status quo bleibt final).
- ~~Bulk-Track-Retro-Audit~~ (welche der 55 skipped Files welcher der 24
  pre-existing Schemas-Migration-Versionen entsprechen) — nur bei
  expliziter Anforderung via Re-Evaluation-Trigger 1.

## Migration-Sketch (Referenz-Anker, bleibt in ADR-007 verankert)

Falls Re-Evaluation-Trigger aktiviert wird:

```sql
-- supabase/migrations/<YYYYMMDD>_schema_migrations_composite_pk.sql
ALTER TABLE supabase_migrations.schema_migrations
  DROP CONSTRAINT IF EXISTS schema_migrations_pkey;

ALTER TABLE supabase_migrations.schema_migrations
  ADD CONSTRAINT schema_migrations_pkey PRIMARY KEY (version, name);
```

**Pre-Activation-Check** _(vor SQL-Run manuell zu verifizieren)_:

```sql
SELECT version, count(*) FROM supabase_migrations.schema_migrations
  GROUP BY version HAVING count(*) > 1;
-- Erwartung: 0 rows. Wenn > 0: vor DROP PK deduplizieren (siehe ADR-002
-- Pattern der manuellen Pre-Edit-Verifikation).
```

`scripts/bulk-track-only-migrations.sh` Diff:

```diff
- sed -i '$ s/,$/ ON CONFLICT (version) DO NOTHING;/' "$SQL_FILE"
+ sed -i '$ s/,$/ ON CONFLICT (version, name) DO NOTHING;/' "$SQL_FILE"
```

## Changelog

- **2026-06-27 — Eröffnung** (Forward-Ticket): Q1-Housekeeping-Audit-Lauf
  zeigte Trade-off-Frage. Status ❌ TODO.
- **2026-06-28 — Decision-Doc erstellt** (Commit `e5acd5b`):
  ADR-007 Trade-off-Analyse empfahl Status-Quo belassen.
  `q1/1.5.1.md` komplett neu geschrieben mit Status 🟢 DECIDED + Body-Rewrite
  - 4 Re-Evaluation-Trigger-Liste + Migration-Sketch-Referenz.
    Audit-Datum auf 2026-06-28 aktualisiert.
- **2026-06-28 — Maintainer-Signoff erhalten** (Commit `f87988f`):
  ADR-007 als ✅ ACCEPTED. 4 Re-Evaluation-Trigger explizit dokumentiert
  in ADR-007 § Konsequenzen + Changelog.
- **2026-06-28 — Ticket-Close** (dieser Commit):
  `q1/1.5.1.md` auf ✅ DONE promoted mit dedizierter
  Acceptance-Resolution-Sektion, die den vollständigen Status-Flow von
  🟢 DECIDED (per ADR-007-Empfehlung) → ADR-007 ACCEPT → ✅ DONE
  nachvollziehbar dokumentiert. Forward-Ticket geschlossen.
