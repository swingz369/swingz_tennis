Table from swingz. Use via `window.Swingz.Table` (bundle loaded from the root `_ds_bundle.js`).

## Related

`TableBody`, `TableCaption`, `TableCell`, `TableFooter`, `TableHead`, `TableHeader`, `TableRow`, `TableSkeleton`
