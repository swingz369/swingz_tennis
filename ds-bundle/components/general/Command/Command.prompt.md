Command from swingz. Use via `window.Swingz.Command` (bundle loaded from the root `_ds_bundle.js`).

## Related

`CommandDialog`, `CommandEmpty`, `CommandGroup`, `CommandInput`, `CommandItem`, `CommandList`, `CommandShortcut`
