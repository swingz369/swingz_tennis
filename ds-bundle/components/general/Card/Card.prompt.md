Card from swingz. Use via `window.Swingz.Card` (bundle loaded from the root `_ds_bundle.js`).

## Related

`CardContent`, `CardDescription`, `CardFooter`, `CardGridSkeleton`, `CardHeader`, `CardSkeleton`, `CardTitle`
